#include "common.h"

float4 main ( p_TL I ) : SV_Target
{
	float4 color = s_base.Sample( smp_base, I.Tex0 );
	color.xyz = lerp( color.rgb, I.Color.rgb, I.Color.a);
	color.a *= I.Color.a;
	return color;
}
