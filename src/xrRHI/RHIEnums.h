#pragma once

enum ERHI_API_LAYER
{
	D3D11,
	D3D9,

	NOT_CREATED = -1
};

enum class ERTColor
{
	Transparent,
	Gray,
	Black
};

enum class ERHI_USAGE
{
	USAGE_DEFAULT = 0,
	USAGE_IMMUTABLE = 1,
	USAGE_DYNAMIC = 2,
	USAGE_STAGING = 3
};

enum class ERHI_DSV_DIMENSION
{
	UNKNOWN = 0,
	TEXTURE1D = 1,
	TEXTURE1DARRAY = 2,
	TEXTURE2D = 3,
	TEXTURE2DARRAY = 4,
	TEXTURE2DMS = 5,
	TEXTURE2DMSARRAY = 6
};

enum class ERHI_VIEW_DIMENSION
{
	Texture2D,
	Texture3D,
	Buffer
};

enum class ERHI_BUFFER_TYPE
{
	VERTEX,
	INDEX,
	CONSTANT
};

enum class ERHI_BUFFER_MAP
{
	READ,
	WRITE,
	WRITE_DISCARD,
	WRITE_NO_OVERWRITE,
	READ_AND_WRITE
};

enum ERHI_CPU_ACCESS_FLAG
{
	ERHI_CPU_ACCESS_FLAG_NONE,
	ERHI_CPU_ACCESS_FLAG_WRITE,
	ERHI_CPU_ACCESS_FLAG_READ
};

enum ERHI_BIND_FLAG
{
	VERTEX_BUFFER = 0x1L,
	INDEX_BUFFER = 0x2L,
	CONSTANT_BUFFER = 0x4L,
	SHADER_RESOURCE = 0x8L,
	STREAM_OUTPUT = 0x10L,
	RENDER_TARGET = 0x20L,
	DEPTH_STENCIL = 0x40L,
	UNORDERED_ACCESS = 0x80L,
	DECODER = 0x200L,
	VIDEO_ENCODER = 0x400L,

	NOT_SET = 0
};
ENUM_CLASS_FLAGS(ERHI_BIND_FLAG);

enum class ERHI_RTV_DIMENSION
{
	UNKNOWN = 0,
	BUFFER = 1,
	TEXTURE1D = 2,
	TEXTURE1DARRAY = 3,
	TEXTURE2D = 4,
	TEXTURE2DARRAY = 5,
	TEXTURE2DMS = 6,
	TEXTURE2DMSARRAY = 7,
	TEXTURE3D = 8
};

enum class ERHI_SHADER_TYPE
{
	PS,
	VS,
	GS,
	HS,
	DS,
	CS,
};

enum class ERHI_CLEAR_TARGET
{
	DEPTH = 1 << 0,
	STENCIL = 1 << 1
};
ENUM_CLASS_FLAGS(ERHI_CLEAR_TARGET);